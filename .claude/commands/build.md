---
description: Drive the Unreal MCP server to build something in the open Unreal Editor from a natural-language prompt.
---

Run the `build` skill at `.claude/skills/build/SKILL.md`. Start with the preflight check to confirm the `unrealMCP` MCP server is connected and the editor is reachable before asking the user what to build.
